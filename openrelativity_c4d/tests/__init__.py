"""Tests for OpenRelativity C4D.

These tests cover the pure-Python physics core only. They must run under a plain
Python interpreter with no Cinema 4D and no third-party packages
(``python -m unittest`` from the repository root).
"""
