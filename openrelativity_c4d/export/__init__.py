"""Metadata export for renderer / post / debugging workflows.

Collects the scene's relativistic state (controller, camera, objects, computed
factors, frame/time) into a JSON document. See
:mod:`openrelativity_c4d.export.metadata_export` and docs/METADATA_SCHEMA.md.
"""
